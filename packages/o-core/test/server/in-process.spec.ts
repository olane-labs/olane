import { NodeState, oAddress, oCore } from '../../src/index.js';
import { expect } from 'chai';

describe('in-process @memory', () => {
  it('should be able to start a single node with no leader', async () => {
    // do nothing
  });
});
