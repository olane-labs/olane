import { oMethod } from '@olane/o-protocol';

export const RAG_PARAMS: { [key: string]: oMethod } = {
  search: {
    name: 'search',
    description: 'Search for data',
    dependencies: [],
    parameters: [],
  },
};
